# Strideo app dependencies. BlazePose (fast path) is CPU-only by design (see
# Dockerfile: MEDIAPIPE_DISABLE_GPU=1). RTMPose-x (full analysis) auto-detects
# CUDA at runtime via src.preprocessing.rtmpose_extractor.detect_device() and
# falls back to CPU automatically — same image/requirements work unmodified
# on a CPU-tier or GPU-tier HF Space; no PyTorch required either way.
#
# Model weights are NOT pip-installable:
#   * MediaPipe .task files  -> models/ directory (see README for download)
#   * RTMPose-x ONNX         -> downloaded automatically to ~/.cache on first run

# Core pipeline
numpy>=1.24.0
scipy>=1.11.0
opencv-python-headless>=4.8.0   # headless: no libGL/GUI deps on a server container
mediapipe==0.10.32              # BlazePose (33 landmarks) — PINNED to the
                                # validated local version. HF previously
                                # resolved 0.10.35, which coincided with a
                                # detection-rate drop (~60%→19%) on the same
                                # clip in headless Linux. Do not unpin without
                                # re-validating detection on HF Spaces.
onnxruntime-gpu[cuda,cudnn]==1.26.0   # RTMPose-x backend. Superset of plain
                                # onnxruntime: registers CUDAExecutionProvider
                                # when the CUDA 12.x/cuDNN 9.x libs it bundles
                                # via the [cuda,cudnn] extra are usable (Linux
                                # only — matches HF Spaces), else only
                                # CPUExecutionProvider, same as the CPU-only
                                # package. Version pinned to match the exact
                                # combination already validated locally on the
                                # Legion RTX 5070 Ti (environment_rtmpose.yml):
                                # onnxruntime-gpu 1.26.0 + cuDNN 9.x.
rtmlib>=0.0.15                  # thin RTMPose wrapper (pure-python, 61 KB)
pydantic>=2.0.0                 # metadata models
requests>=2.31.0                # contact form POST + reCAPTCHA siteverify call

# Web UI + reporting
streamlit==1.54.0                # PINNED: st.components.v2 (reCAPTCHA widget)
                                 # has had breaking signature changes across
                                 # 2025->2026 releases; pin to the version this
                                 # was built and tested against, same rationale
                                 # as the mediapipe pin above.
plotly>=6.0.0                   # StrideoNet-vs-engine metric charts + Pace
                                 # Analysis (results.py) — a speculative add
                                 # until those charts gave it real
                                 # consumers 2026-08-27/28; confirmed kept,
                                 # not orphaned
reportlab>=4.0.0
